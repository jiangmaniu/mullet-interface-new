import { LangPageParam } from '@/locales/init-lingui'

export type TradeSymbolPageParams = { symbol: string }

export default function TradeSymbolLayout({ children }: { children: React.ReactNode }) {
  return children
}
