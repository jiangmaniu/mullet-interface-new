git_url("https://github.com/jiangmaniu/mullet-interface-new.git")
git_branch("certificates")
storage_mode("git")

type("development")

app_identifier([
  "com.mullet.app",
  "com.mullet.app.dev",
  "com.mullet.app.test"
])

team_id(ENV["TEAM_ID"])
# username 不在此处设置，由 Fastfile 根据是否需要验证证书来决定
